<div class="page page--webinar-registration">
  <div class="container">
    <!-- Page header -->
    <div class="page-hero">
      <p class="hero__overline">Customer Projects</p>
      <h1 class="page-hero__heading">Webinar Registration</h1>
      <p class="page-hero__desc">
        Register attendees for a Vimeo-powered webinar. Enter your event
        credentials below, or leave them blank to use the server defaults from
        <code>.env</code>.
      </p>
    </div>

    <!-- API / event config -->
    <div class="card">
      <form id="configForm" novalidate>
        <div class="form-group">
          <label class="form-group__label" for="tokenInput">
            Personal Access Token
            <span class="form-group__optional">(optional — overrides server default)</span>
          </label>
          <input
            type="text"
            id="tokenInput"
            class="form-group__input"
            placeholder="Your Token must have the CAPABILITY_ACCESS_LEADS capability."
            autocomplete="off"
            spellcheck="false" />
        </div>
        <div class="form-group">
          <label class="form-group__label" for="eventIdInput">
            Webinar Event ID
            <span class="form-group__optional">(optional — overrides server default)</span>
          </label>
          <input
            type="text"
            id="eventIdInput"
            class="form-group__input"
            placeholder="Your Event must be scheduled and accepting registrations."
            autocomplete="off"
            spellcheck="false" />
        </div>
        <p id="configError" class="form-group__error hidden"></p>
        <p id="configStatus" class="form-group__hint hidden"></p>
        <button type="submit" class="btn btn--primary right">
          Apply Config
        </button>
      </form>
    </div>

    <!-- Attendee registration form -->
    <div class="card">
      <form id="regForm" novalidate>
        <div class="form-group">
          <label class="form-group__label" for="firstNameInput">First Name</label>
          <input
            type="text"
            id="firstNameInput"
            class="form-group__input"
            placeholder="Jane"
            autocomplete="given-name" />
        </div>
        <div class="form-group">
          <label class="form-group__label" for="lastNameInput">Last Name</label>
          <input
            type="text"
            id="lastNameInput"
            class="form-group__input"
            placeholder="Smith"
            autocomplete="family-name" />
        </div>
        <div class="form-group">
          <label class="form-group__label" for="emailInput">Email Address</label>
          <input
            type="email"
            id="emailInput"
            class="form-group__input"
            placeholder="jane@example.com"
            autocomplete="email" />
        </div>
        <p id="regError" class="form-group__error hidden"></p>
        <button type="submit" id="registerBtn" class="btn btn--primary right">
          Register
        </button>
      </form>

      <!-- Shown after successful registration -->
      <div id="regConfirmed" class="status hidden">
        <p class="status__icon status__icon--success">✓</p>
        <p class="status__heading">You're Registered!</p>
        <p class="status__body">
          You will receive a reminder email 24 hours before the webinar starts.
          See you there!
        </p>
      </div>

      <!-- Shown when duplicate email detected -->
      <div id="alreadyReg" class="status hidden">
        <p class="status__icon status__icon--info">i</p>
        <p class="status__heading">Already Registered</p>
        <p class="status__body">
          This email address is already signed up for this event.
        </p>
      </div>
    </div>
  </div>
  <!-- /.container -->
</div>
<!-- /.page -->

<!-- Toast container lives outside the main flow so it can be fixed-position -->
<div
  id="toastContainer"
  class="toast-container"
  role="status"
  aria-live="polite"></div>
